##############################################
# Tipos de Documentos
##############################################

type DocumentoResponse {
  type: String!
  message: String!
  data: String
}

extend type Query {
  getCarnetSolvencia(
    token: String!
    tipoDeDocumento: TipoDeDocumento!
    suscripcionId: Int
  ): DocumentoResponse!
}
