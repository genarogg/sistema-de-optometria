schema: http://localhost:4000/graphql
generates:
  ../global/enums.ts:
    plugins:
      - typescript
    config:
      onlyEnums: true
      enumsAsTypes: false
      namingConvention:
        enumValues: upper-case#upperCase
